# Interface boundary (§6.8) for plate_chain.workflow.yaml: the sample plate starts
# on the loader and the final plate is delivered to the unloader slot. The Int
# boundary ports (start / result) are Pure Data and occupy no spot, so they are
# not pinned here.
interface:
  inputs:
    sample: loader.stage
  outputs:
    plate_final: unloader.slot
activities: []
